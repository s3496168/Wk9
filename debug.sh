#!/bin/sh -x
# -x displays each line with all variables expanded
#prior to execution

echo "Always remember that echo statements can be used to debug too"

#designed to throw an error-$FOO is undefined
#if [ $FOO = bar } ; then

    #printf "foo is bar\n"
#fixed
#fi
+ "[" =bar "]"

    + printf "foo is bar\n"
foo is bar
